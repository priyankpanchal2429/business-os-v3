{
    "name": "business-os-v3",
    "private": true,
    "scripts": {
        "dev": "turbo dev",
        "build": "turbo build",
        "lint": "turbo lint",
        "test": "turbo test",
        "clean": "rm -rf node_modules **/*/node_modules"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "engines": {
        "node": ">=18"
    },
    "packageManager": "npm@10.0.0",
    "devDependencies": {
        "turbo": "^2.8.1"
    }
}
